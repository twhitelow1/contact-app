json = []
index = 0
while index < @contacts.length
  contact = @contacts[index]
  contact_hash = {
    id: contact.id,
    title: contact.first_name,
  }
  json < contact_hash
  index += 1
end

return json
